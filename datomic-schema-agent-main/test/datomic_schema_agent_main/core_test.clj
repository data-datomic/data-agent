(ns datomic-schema-agent-main.core-test
  (:require [clojure.test :refer :all]
            [datomic-schema-agent-main.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
