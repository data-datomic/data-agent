(ns datomic-schema-agent.config.agent-configuration
  )

;; Global Configuration properties for Schema-Agent

;; For now to read in and generate a configuration object from the following locations:
;;   1) A JSON configuration file
;;   2) A CLI
;;      - where/how to handle the packaging/depackaging of the CLI parameters? Not here IMO....
